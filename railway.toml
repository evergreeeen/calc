[deploy]
startCommand = "bash -c 'python -m uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/api/options"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
